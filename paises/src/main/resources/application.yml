server:
  port: 8089
springdoc:
  api-docs:
    path: /paises-openapi
spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    initialization-mode: always
    platform: postgres
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
